---网格

LDGrid = {}
local width = 10
local length = 10
--===================================================
function LDGrid.init(x, y)
    width = x
    length = y
end

-- index转成vector2
function LDGrid.index2vector(index)

end

-- vector2转成index
function LDGrid.vector2index(v2)

end
--===================================================
return LDGrid
